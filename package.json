{
  "name": "vibe-kick",
  "private": true,
  "packageManager": "bun@1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun --cwd apps/web dev",
    "build": "bun --cwd apps/web build",
    "preview": "bun --cwd apps/web preview",
    "test": "bun test --cwd packages/game-core",
    "test:e2e": "bun test --cwd apps/web tests/e2e"
  },
  "dependencies": {
    "@types/bun": "^1.3.6"
  }
}
