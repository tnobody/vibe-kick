{
  "name": "vibe-kick",
  "private": true,
  "packageManager": "bun@1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun --cwd apps/web dev",
    "build": "bun --cwd apps/web build",
    "preview": "bun --cwd apps/web preview",
    "test": "vitest --config packages/game-core/vitest.config.ts"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
